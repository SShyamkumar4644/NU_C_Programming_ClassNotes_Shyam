#include <stdio.h>

int main() {
    char letter = 'a';  // Initialize a character variable
    printf("Original letter: %c\n", letter);

    letter++;  // Increment the letter
    printf("Incremented letter: %c\n", letter);

    return 0;
}
