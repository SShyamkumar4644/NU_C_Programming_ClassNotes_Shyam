// Function with No Arguments and No Return Value

#include <stdio.h>

void greet() {
    printf("Hello! Welcome to the program.\n");
}

int main() {
    greet(); // No arguments passed, no return value
    return 0;
}

// The greet() function has no parameters and no return type (void), 
// so it just prints a message when called.